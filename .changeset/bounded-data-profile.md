---
"klopsi": minor
---

Add the experimental `klopsi profile` command for bounded schema, completeness, distinct-count, numeric-summary, and categorical-frequency orientation over every tabular input supported by the existing DuckDB stage.
