{
  "name": "klopsi-cli-workspace",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.11.0",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm build && pnpm -r typecheck",
    "test": "pnpm build && pnpm test:unit && pnpm test:integration && pnpm test:e2e",
    "test:unit": "vitest run --project unit",
    "test:integration": "vitest run --project integration",
    "test:e2e": "vitest run --project cli-e2e apps/cli/test/*.e2e.test.ts",
    "test:pack": "vitest run --project cli-e2e apps/cli/test/pack.test.ts",
    "lint": "eslint .",
    "format:check": "prettier --check .",
    "pack:cli": "pnpm build && npm pack ./apps/cli --json",
    "check": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm test && pnpm test:pack"
  },
  "devDependencies": {
    "@changesets/cli": "2.31.0",
    "@eslint/js": "10.0.1",
    "@types/node": "24.13.3",
    "@vitest/coverage-v8": "4.1.10",
    "eslint": "10.6.0",
    "prettier": "3.9.5",
    "tsup": "8.5.1",
    "typescript": "6.0.3",
    "typescript-eslint": "8.63.0",
    "vitest": "4.1.10"
  }
}
