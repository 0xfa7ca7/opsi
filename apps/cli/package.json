{
  "name": "@opsi/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "opsi": "dist/main.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup --config tsup.config.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
