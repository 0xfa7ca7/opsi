# opsi

Production Node.js 24 CLI and TypeScript SDK for Slovenian public data. The package has no AI features and no telemetry. Install with `npm install --global opsi`, run `opsi doctor --offline`, and see the repository README and documentation for commands, supported platforms, configuration, and security.
